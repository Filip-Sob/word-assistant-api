spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/word_assistant
    username: ${DB_USER:word_assistant}
    password: ${DB_PASS:word123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: 8080

openai:
  api-key: ${OPENAI_API_KEY}
  model: ${OPENAI_MODEL:gpt-4.1}
